#include <stdio.h>
int main ()
{
    int n,i;
    printf("enter the element to find the factor");
    scanf("%d", &n);
    printf("factor of %d are : \t",n)
    for(i=1;i<=n;i++)
    {
        if(n%i==0)
        {
            printf("%d",i);
        }
    }
 return 0;
}
